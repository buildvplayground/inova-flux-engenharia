# Bloqueia acesso web direto aos currículos enviados (privacidade / LGPD).
# Os arquivos ficam acessíveis apenas no servidor (não são baixáveis pela web).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
